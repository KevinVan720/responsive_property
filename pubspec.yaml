name: responsive_property
description: Inspired by Flutter's MaterialProperty, gives you a responsive value based on the current screen dimension.
version: 0.0.3
homepage: https://github.com/KevinVan720/responsive_property

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_class_parser: ^0.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

