name: responsive_property
description: A new Flutter package.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_class_parser: ^0.2.2


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

